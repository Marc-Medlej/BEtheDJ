<div class="container">
  <%= simple_form_for [@venue, @event] do |f| %>
  <%= f.input :name %>
  <%= f.input :date %>
  <%= f.button :submit, class: "btn" %>
  <% end %>

<!-- link_to "Back", ??  -->
</div>

